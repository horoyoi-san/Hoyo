## Web

Website: srtools.pages.dev

## Compiling

1. Compile with `./gradlew jar` from your system terminal
2. Place the compiled jar file (`json-tools.jar`) into your LunarCore plugins folder

## Usage

Use `/json-export [File Name]` in-game