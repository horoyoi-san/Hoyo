# JWT Secret - 请设置一个强密码作为JWT签名密钥
JWT_SECRET=your-secret-key

# SMTP Configuration - 请配置您的邮件服务器信息
SMTP_HOST=your_smtp_host
SMTP_PORT=465
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password

# Database
DATABASE_URL=./netdisk.db

# Server
PORT=8080

# Frontend URL (用于CORS配置和分享链接生成)
# 开发环境: http://localhost:3000
# 生产环境: https://your-frontend-domain.com
FRONTEND_URL=http://localhost:3000

# Backend URL (用于生成下载链接和直链)
# 开发环境: http://localhost:8080
# 生产环境: https://your-backend-domain.com
# 注意: 如果使用反向代理，可以留空让系统自动检测
BACKEND_URL=http://localhost:8080

# 配置说明：
# 1. JWT_SECRET: 用于JWT token签名的密钥，建议使用32位以上的随机字符串
# 2. SMTP_HOST: 邮件服务器地址，如 smtp.gmail.com, smtp.qq.com 等
# 3. SMTP_PORT: 邮件服务器端口，通常SSL为465，TLS为587
# 4. SMTP_USER: 邮件服务器用户名，通常是完整的邮箱地址
# 5. SMTP_PASS: 邮件服务器密码或应用专用密码
# 6. DATABASE_URL: SQLite数据库文件路径
# 7. PORT: 后端服务运行端口
# 8. FRONTEND_URL: 前端应用地址，用于CORS配置和分享链接生成
#    - 开发环境: http://localhost:3000
#    - 生产环境: https://your-frontend-domain.com
# 9. BACKEND_URL: 后端应用地址，用于生成下载链接和直链
#    - 开发环境: http://localhost:8080
#    - 生产环境: https://your-backend-domain.com
#    - 如果使用 nginx 反向代理，可以留空让系统自动从请求头检测域名
